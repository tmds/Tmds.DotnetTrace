namespace Tmds.Babeltrace
{
    public enum CftScope
    {
        PacketHeader = 0,
        PacketContext = 1,
        StreamEventHeader = 2,
        StreamEventContext = 3,
        EventContext = 4,
        EventFields = 5
    }
}